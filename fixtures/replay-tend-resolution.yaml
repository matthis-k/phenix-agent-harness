id: replay-tend-resolution
request: "Run tend checks in phenix-stitch submodule."
expected:
  tend_resolution:
    order:
      - nix run .#tend
      - nix develop . --command tend
      - ambient tend
    prefer_mcp: true
    fallback_to_cli: true
  notes: Stitch builtin_tend_check tries nix run first, then nix develop, then ambient path.
