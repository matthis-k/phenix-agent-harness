{
  "commands": {
    "test": [
      "nix build .#phenix-runtime-tests --no-link",
      "nix build .#phenix-repository-checks --no-link"
    ],
    "build": ["nix build .#phenix-pi-package --no-link"]
  }
}
