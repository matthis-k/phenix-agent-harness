[package]
name = "phenix-conductor"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["builtin-runtime"]
builtin-runtime = ["dep:phenix-acp-runtime"]

[dependencies]
agent-client-protocol.workspace = true
base64.workspace = true
clap.workspace = true
phenix-acp = { path = "../phenix-acp" }
phenix-acp-backend = { path = "../phenix-acp-backend" }
phenix-acp-runtime = { path = "../phenix-acp-runtime", optional = true }
phenix-runtime-api = { path = "../phenix-runtime-api" }
serde.workspace = true
serde_json.workspace = true
shell-words.workspace = true
tokio.workspace = true
