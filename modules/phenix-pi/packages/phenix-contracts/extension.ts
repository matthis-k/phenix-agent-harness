import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Contracts extension: installs the generic handoff schema framework package. */
export default function phenixContracts(_pi: ExtensionAPI): void {
  // Concrete contract definitions are provided by suites or user-local configuration.
}
