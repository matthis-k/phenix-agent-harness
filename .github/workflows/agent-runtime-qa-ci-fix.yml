name: Agent runtime QA CI fix

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Apply compile and recovery fixes
        run: |
          python3 -m py_compile .github/agent-runtime-qa-ci-fix.py
          python3 .github/agent-runtime-qa-ci-fix.py
          cargo fmt --manifest-path rust/Cargo.toml --all
          git diff --check
      - name: Commit product changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add rust/crates/phenix-backend-native/src/lib.rs \
                  rust/crates/phenix-conductor/tests/black_box_model_tool_loop.rs
          git diff --cached --stat
          git commit -m 'fix: harden tool-call recovery and native typing'
          git push
