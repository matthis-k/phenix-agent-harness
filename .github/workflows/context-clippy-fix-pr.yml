name: context-clippy-fix-pr

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/context-and-skills' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/context-and-skills
          fetch-depth: 0
      - run: python3 .github/context-clippy-fix.py
      - run: cargo fmt --manifest-path rust/Cargo.toml --all
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add rust/crates/phenix-conductor/src/context.rs
          git commit -m 'fix: initialize context registry directly'
          git push origin HEAD:agent/context-and-skills
