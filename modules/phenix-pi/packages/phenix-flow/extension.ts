import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Flow extension: installs the generic workflow framework package. */
export default function phenixFlow(_pi: ExtensionAPI): void {
  // Workflow definitions are provided by suites or user-local configuration.
}
