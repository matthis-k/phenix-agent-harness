diff --git a/src/core/tee.rs b/src/core/tee.rs
index e366a31..e6af712 100644
--- a/src/core/tee.rs
+++ b/src/core/tee.rs
@@ -42,6 +42,10 @@ fn cleanup_old_files(dir: &std::path::Path, max_files: usize) {
     }
 }
 
+pub(crate) fn phenix_lossless() -> bool {
+    std::env::var("PHENIX_RTK_LOSSLESS").ok().as_deref() == Some("1")
+}
+
 /// Check if tee should be skipped based on config, mode, exit code, and size.
 /// Returns None if should skip, Some(tee_dir) if should proceed.
 fn should_tee(
@@ -51,6 +55,10 @@ fn should_tee(
     exit_code: i32,
     tee_dir: Option<PathBuf>,
 ) -> Option<PathBuf> {
+    if phenix_lossless() {
+        return tee_dir;
+    }
+
     if !config.enabled {
         return None;
     }
@@ -88,7 +96,9 @@ fn write_tee_file(
     let filepath = tee_dir.join(filename);
 
     // Truncate at max_file_size (find a safe UTF-8 char boundary)
-    let content = if raw.len() > max_file_size {
+    let content = if phenix_lossless() {
+        raw.to_string()
+    } else if raw.len() > max_file_size {
         let boundary = raw
             .char_indices()
             .take_while(|(i, _)| *i < max_file_size)
diff --git a/src/core/runner.rs b/src/core/runner.rs
index e2a3ad5..db7c96e 100644
--- a/src/core/runner.rs
+++ b/src/core/runner.rs
@@ -106,6 +106,10 @@ where
     let raw = &result.raw;
     let raw_stdout = &result.raw_stdout;
 
+    if crate::core::tee::phenix_lossless() {
+        let _ = crate::core::tee::tee_raw(raw, cmd_label, exit_code);
+    }
+
     if opts.skip_filter_on_failure && exit_code != 0 {
         if !result.raw_stdout.trim().is_empty() {
             print!("{}", result.raw_stdout);
@@ -188,6 +192,10 @@ pub fn run(
                 stream::run_streaming(&mut cmd, StdinMode::Null, FilterMode::Streaming(filter))
                     .with_context(|| format!("Failed to run {}", tool_name))?;
 
+            if crate::core::tee::phenix_lossless() {
+                let _ = crate::core::tee::tee_raw(&result.raw, &cmd_label, result.exit_code);
+            }
+
             if let Some(label) = opts.tee_label {
                 if let Some(hint) =
                     crate::core::tee::tee_and_hint(&result.raw, label, result.exit_code)
@@ -210,6 +218,10 @@ pub fn run(
                 stream::run_streaming(&mut cmd, StdinMode::Inherit, FilterMode::Passthrough)
                     .with_context(|| format!("Failed to run {}", tool_name))?;
 
+            if crate::core::tee::phenix_lossless() {
+                let _ = crate::core::tee::tee_raw(&result.raw, &cmd_label, result.exit_code);
+            }
+
             timer.track_passthrough(&cmd_label, &format!("rtk {} (passthrough)", cmd_label));
             Ok(result.exit_code)
         }
