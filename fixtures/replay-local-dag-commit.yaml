id: replay-local-dag-commit
request: "Commit the verified submodule changes locally."
expected:
  route: committer
  lease: LocalCommit
  local_dag_mode: true
  approvals_expected: 1
  checkpoints:
    - staged_files
    - commit_summary
